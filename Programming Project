# os gives us access to the command line, time is used as a delay
import os, time

# this function prints text delayed like older video games
def slowText(text, delay=0.03):
    for char in text:
        print(char, end='', flush=True)
        time.sleep(delay)
    print()  

# introduces the player, sets the playerName variable
def start():
    global playerName   # establishes playerName as a global variable
    os.system('cls' if os.name == 'nt' else 'clear')
    slowText("Good morning! Please enter your name: ")
    playerName = input()
    slowText("Hello, {}! You need to get dressed for school.".format(playerName))
    # Further game logic would go here
    bedroom()

# continuation of the start, allows for the player to go to the living room
def bedroom():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))  
    slowText("You are in your bedroom.")
    slowText("From here, you can go to the bedroom closet, coat closet, dresser, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "bedroom closet":
        bedroomCloset()
    elif choice == "coat closet":
        coatCloset()
    elif choice == "dresser":
        dresser()
    elif choice == "shoe rack":
        shoeRack()

    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        bedroom()

def coatCloset():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the coat closet. You look at shoes and outer layers.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "bedroom":
        bedroom()
    elif choice == "open window":  
        openWindow() 
    elif choice == "look for items":
        if searchSelections(selections, "paper clip"):
            slowText("You decided to scan the room. And you found nothing.")
        else:
            slowText("You decided to scan the room. You open a junk drawer and find a paper clip. Do you want to pick it up?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("paper clip")
                print(selections) # this line is only for testing
        time.sleep(2)
        kitchen()    
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        kitchen()

def dresser():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the dresser. You can select socks or underwear")
    slowText("or you can go to the bedroom closet, coat closet, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "living room":
        livingRoom()
    elif choice == "look for items":
        if searchSelections(selections, "boots"):
            slowText("You decided to scan the room. And you found nothing.")
        else:
            slowText("You decided to scan the room. You see boots. Do you want to pick them up?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("boots")
                print(selections) # this line is only for testing
        time.sleep(2)
        garden()   
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        garden()

def bedroomCloset():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the bedroom closet. You can select pants or a shirt")
    slowText("or you can go to the coat closet, dresser, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "coat closet":
        coatCloset()
    elif choice == "pants":
        if searchSelections(selections, "pants"):
            slowText("You already selected your pants.")
        else:
            slowText("You decided to scan the closet. You found some cool pants. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("pants")
                print(selections) # this line is only for testing
        time.sleep(2)
        bedroom()   
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        bedroom()

def searchSelections(pack, item):
    # this function should return true if item is inside the list named 'pack'
    found = False
    for i in range(len(pack)):
        if pack[i] == item:
            found = True
    return found

def openDoor():
    slowText("You decided to try to open the door")
    if searchSelections(selections, "computer science book") == True:
        slowText("You used the computer science book to keep the window open. Now it is open.")
        slowText("You see a huge mud pit in the yard outside.")
        if searchSelections(selections, "boots") == True:
            slowText("You put on your puts to safely walk through the mud.")
            if searchSelections(selections, "oreos") == True:
                slowText("You even have Oreos as a snack")
            else:  
                slowText("Bummer you don't have any Oreos to eat.")
        else:
            slowText("You can't walk through the mud right now.")
            time.sleep(3)
            kitchen()
    else:
        slowText("Unfortunately, you are too cold to go outside.")
        time.sleep(3)
        kitchen() 

playerName = ""
selections = []
start()
