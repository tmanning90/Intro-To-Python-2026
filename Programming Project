# os gives us access to the command line, time is used as a delay
import os, time

# this function prints text delayed like older video games
def slowText(text, delay=0.02):
    for char in text:
        print(char, end='', flush=True)
        time.sleep(delay)
    print()  

# introduces the player, sets the playerName variable
def start():
    global playerName   # establishes playerName as a global variable
    os.system('cls' if os.name == 'nt' else 'clear')
    slowText("Good morning! Please enter your name: ")
    playerName = input()
    slowText("Hello, {}! You need to get dressed for school.".format(playerName))
    # Further game logic would go here
    bedroom()

# continuation of the start, allows for the player to go anywhere
def bedroom():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))  
    slowText("You are in your bedroom.")
    slowText("From here, you can go to the bedroom closet, coat closet, dresser, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "bedroom closet":
        bedroomCloset()
    elif choice == "coat closet":
        coatCloset()
    elif choice == "dresser":
        dresser()
    elif choice == "shoe rack":
        shoeRack()

    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        bedroom()

def coatCloset():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the coat closet. You can select a coat, hat, or gloves")
    slowText("or you can go to the bedroom closet, dresser, or shoe rack.")
    slowText("You may also leave the house.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "bedroom closet":
        bedroomCloset()
    elif choice == "dresser":  
        dresser() 
    elif choice == "shoe rack":  
        shoeRack()
    elif choice == "leave the house":
        leave()

    elif choice == "coat":
        if searchSelections(selections, "coat"):
            slowText("You already selected your coat.")
        else:
            slowText("You decided to scan the closet. You found a coat. Do you want to select it?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("coat")
                print(selections) # this line is only for testing
        time.sleep(2)
        coatCloset()    
    elif choice == "gloves":
        if searchSelections(selections, "gloves"):
            slowText("You already selected your gloves.")
        else:
            slowText("You see gloves. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("gloves")
                print(selections) # this line is only for testing
        time.sleep(2)
        coatCloset()   

    elif choice == "hat":
        if searchSelections(selections, "hat"):
            slowText("You already selected your hat.")
        else:
            slowText("You also see a hat. Do you want to select it?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("hat")
                print(selections) # this line is only for testing
        time.sleep(2)
        coatCloset()   

    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        coatCloset()

def dresser():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the dresser. You can select socks or underwear")
    slowText("or you can go to the bedroom closet, coat closet, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "bedroom closet":
        bedroomCloset()
    elif choice == "coat closet":
        coatCloset()
    elif choice == "shoe rack":
        shoeRack()

    elif choice == "socks":
        if searchSelections(selections, "socks"):
            slowText("You already selected your socks.")
        else:
            slowText("You see some snazzy socks. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("socks")
                print(selections) # this line is only for testing
        time.sleep(2)
        dresser()   

    elif choice == "underwear":
        if searchSelections(selections, "underwear"):
            slowText("You already selected your underwear.")
        else:
            slowText("You need underwear. You see underwear. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("underwear")
                print(selections) # this line is only for testing
        time.sleep(2)
        dresser()  

    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        dresser()

def bedroomCloset():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the bedroom closet. You can select pants or a shirt")
    slowText("or you can go to the coat closet, dresser, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "coat closet":
        coatCloset()
    elif choice == "dresser":
        dresser()
    elif choice == "shoe rack":
        shoeRack()

    elif choice == "pants":
        if searchSelections(selections, "pants"):
            slowText("You already selected your pants.")
        else:
            slowText("You decided to scan the closet. You found some cool pants. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("pants")
                print(selections) # this line is only for testing
        time.sleep(2)
        bedroomCloset()   

    elif choice == "shirt":
        if searchSelections(selections, "shirt"):
            slowText("You already selected your shirt.")
        else:
            slowText("You decided to scan the closet. You found a cool shirt. Do you want to select it?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("shirt")
                print(selections) # this line is only for testing
        time.sleep(2)
        bedroomCloset()   
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        bedroomCloset()

def shoeRack():
    global selections
    os.system('cls' if os.name == 'nt' else 'clear')
    print("selections: {}".format(selections))
    slowText("You are at the shoe rack. You can select sneakers or boots")
    slowText("or you can go to the bedroom closet, coat closet or dresser.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "bedroom closet":
        bedroomCloset()
    if choice == "coat closet":
        coatCloset()
    if choice == "dresser":
        dresser()

    elif choice == "boots":
        if searchSelections(selections, "boots"):
            slowText("You already selected your socks.")
        else:
            slowText("You see boots. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("boots")
                print(selections) # this line is only for testing
        time.sleep(2)
        shoeRack()   

    elif choice == "sneakers":
        if searchSelections(selections, "sneakers"):
            slowText("You already selected your sneakers.")
        else:
            slowText("You need shoes. You see sneakers. Do you want to select them?")
            choice = input().strip().lower()
            if choice == "yes":
                selections.append("sneakers")
                print(selections) # this line is only for testing
        time.sleep(2)
        shoeRack()   
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        bedroom()

def searchSelections(pack, item):
    # this function should return true if item is inside the list named 'pack'
    found = False
    for i in range(len(pack)):
        if pack[i] == item:
            found = True
    return found

def leave():
    slowText("You open the door to leave and realize it's really cold outside.")
   
    if searchSelections(selections, "boots") == True:
        slowText("Luckily you have boots on.")
        if searchSelections(selections, "hat") == True:
            slowText("Luckly, you have gloves.")
            if searchSelections(selections, "hat") == True:
                slowText("You even have a hat.")
            else:  
                slowText("Bummer you don't have a hat.")
                if searchSelections(selections, "coat") == True:
                    slowText("Fortunately, you have a coat.")
                else:
                    slowText("Oh boy, you need a coat.")
        else:
            slowText("You are not dressed ready to leave.")
            time.sleep(3)
            coatCloset()
    else:
        slowText("Unfortunately, you are not dressed ready to leave right now.")
        time.sleep(1.5)
        coatCloset() 

playerName = ""
selections = []
start()
