# os gives us access to the command line, time is used as a delay
import os, time

# this function prints text delayed like older video games
def slowText(text, delay=0.06):
    for char in text:
        print(char, end='', flush=True)
        time.sleep(delay)
    print()  

# introduces the player, sets the playerName variable
def start():
    global playerName   # establishes playerName as a global variable
    os.system('cls' if os.name == 'nt' else 'clear')
    slowText("Welcome to the game! Please enter your name: ")
    playerName = input()
    slowText("Hello, {}! You need to get dressed for school.".format(playerName))
    slowText("From here, you can go to the bedroom closet, coat closet, dresser, or shoe rack.")
    # Further game logic would go here
    bedroom()

# continuation of the start, allows for the player to go to the living room
def bedroom():
    global backpack
    os.system('cls' if os.name == 'nt' else 'clear')
    print("backpack: {}".format(backpack))
    slowText("You are in your bedroom. ")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "kitchen":
        kitchen()
    elif choice == "bedroom":
        bedroom()
    elif choice == "garden":
        garden()
    elif choice == "look for items":
        if searchBackpack(backpack, "oreos"):
            slowText("You decided to scan the room. And you found nothing.")
        else:
            slowText("You decided to scan the room. You find a half-eaten pack of oreos. Do you want to pick them up?")
            choice = input().strip().lower()
            if choice == "yes":
                backpack.append("oreos")
                print(backpack) # this line is only for testing
        time.sleep(2)
        bedroom()
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        livingRoom()

def kitchen():
    global backpack
    os.system('cls' if os.name == 'nt' else 'clear')
    print("backpack: {}".format(backpack))
    slowText("You are in the kitchen. There is a door to the living room and a window. You can also look for items.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "living room":
        livingRoom()
    elif choice == "open window":
        openWindow()
    elif choice == "look for items":
        if searchBackpack(backpack, "paper clip"):
            slowText("You decided to scan the room. And you found nothing.")
        else:
            slowText("You decided to scan the room. You open a junk drawer and find a paper clip. Do you want to pick it up?")
            choice = input().strip().lower()
            if choice == "yes":
                backpack.append("paper clip")
                print(backpack) # this line is only for testing
        time.sleep(2)
        kitchen()    
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        kitchen()

def garden():
    global backpack
    os.system('cls' if os.name == 'nt' else 'clear')
    print("backpack: {}".format(backpack))
    slowText("You are in the garden. There is a door to the living room. You can also look for items.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "living room":
        livingRoom()
    elif choice == "look for items":
        if searchBackpack(backpack, "boots"):
            slowText("You decided to scan the room. And you found nothing.")
        else:
            slowText("You decided to scan the room. You see boots. Do you want to pick them up?")
            choice = input().strip().lower()
            if choice == "yes":
                backpack.append("boots")
                print(backpack) # this line is only for testing
        time.sleep(2)
        garden()   
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        garden()

def bedroomCloset():
    global backpack
    os.system('cls' if os.name == 'nt' else 'clear')
    print("backpack: {}".format(backpack))
    slowText("You are at the bedroom closet.  You can go to the coat closet, dresser, or shoe rack.")
    slowText("What would you like to do?")
    choice = input().strip().lower()
    if choice == "living room":
        livingRoom()
    elif choice == "look for items":
        if searchBackpack(backpack, "computer science book"):
            slowText("You decided to scan the room. And you found nothing.")
        else:
            slowText("You decided to scan the room. You found a computer science book on the floor. Do you want to pick it up?")
            choice = input().strip().lower()
            if choice == "yes":
                backpack.append("computer science book")
                print(backpack) # this line is only for testing
        time.sleep(2)
        bedroom()   
    else:
        print("Invalid choice. Please try again.")
        time.sleep(3)
        bedroom()

def searchBackpack(pack, item):
    # this function should return true if item is inside the list named 'pack'
    found = False
    for i in range(len(pack)):
        if pack[i] == item:
            found = True
    return found

def openWindow():
    slowText("You decided to try to open the window")
    if searchBackpack(backpack, "computer science book") == True:
        slowText("You used the computer science book to keep the window open. Now it is open.")
        slowText("You see a huge mud pit in the yard outside.")
        if searchBackpack(backpack, "boots") == True:
            slowText("You put on your puts to safely walk through the mud.")
            if searchBackpack(backpack, "oreos") == True:
                slowText("You even have Oreos as a snack")
            else:  
                slowText("Bummer you don't have any Oreos to eat.")
        else:
            slowText("You can't walk through the mud right now.")
            time.sleep(3)
            kitchen()
    else:
        slowText("Unfortunately, you can't unlock the window.")
        time.sleep(3)
        kitchen() 

playerName = ""
backpack = []
start()
